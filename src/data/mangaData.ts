import { MangaNovelItem } from '../types';

// Empty by default as requested. The user will add real manga and novels via Admin Dashboard or Firebase.
export const INITIAL_MANGA_NOVELS: MangaNovelItem[] = [];
